# ==========================================================================
# data source（shared）
# ==========================================================================

# 実行中の AWS アカウント情報。
# Cognito Managed Login Domain の prefix は AWS 全体でグローバル一意である必要があるため、
# Account ID を suffix に付けて衝突を避ける（値はハードコードしない）。
data "aws_caller_identity" "current" {}
